{
	"name": "aipodcast-worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"deploy:pages": "wrangler pages deploy ../pages --project-name=ai-roundtable",
		"deploy:all": "npm run deploy && npm run deploy:pages",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.42.2"
	},
	"dependencies": {
		"@cloudflare/ai": "^1.2.2"
	}
}
